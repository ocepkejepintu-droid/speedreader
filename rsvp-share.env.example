# Copy to /etc/rsvp-share.env on the VPS (chmod 640, root:nginx).
# The share server is intentionally rate-limited per IP (30 req / 60s) and
# does NOT require auth — the v1.1 demo is single-user / single-IP.
RSVP_SHARE_PORT=9878
RSVP_SHARE_DIR=/var/www/rsvp-reader/share-data

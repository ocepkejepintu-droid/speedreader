# Copy to /etc/rsvp-reader.env on the VPS (chmod 600).
# Must match NEXTAUTH_SECRET on zipang.id (or set the same value in both places).
RSVP_SYNC_JWT_SECRET=REPLACE_WITH_NEXTAUTH_SECRET
RSVP_SYNC_DIR=/var/www/rsvp-reader/sync-data
RSVP_SYNC_PORT=9877